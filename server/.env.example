# Port the server listens on (Render sets this automatically).
PORT=3001

# Allowed CORS origin — set to your Amplify frontend URL in production, e.g.:
# CLIENT_ORIGIN=https://main.d1abc123.amplifyapp.com
CLIENT_ORIGIN=*
