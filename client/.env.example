# URL of the deployed backend (Render).
# In development this is NOT needed — Vite's proxy handles it.
# In production set this to your Render service URL, e.g.:
# VITE_SERVER_URL=https://codebattle-server.onrender.com
VITE_SERVER_URL=
